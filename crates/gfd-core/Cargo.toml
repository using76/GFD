[package]
name = "gfd-core"
version = "0.1.0"
edition = "2021"
description = "Core foundational types and traits for the GFD solver framework"

[dependencies]
serde = { version = "1", features = ["derive"] }
nalgebra = "0.33"
nalgebra-sparse = "0.10"
thiserror = "2"
approx = "0.5"
